server {server {server {

    listen 80;

    server_name localhost;    listen 3000;    listen 80;

    root /usr/share/nginx/html;

    index index.html;    server_name localhost;    server_name localhost;



    gzip on;    root /usr/share/nginx/html;    root /usr/share/nginx/html;

    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    index index.html;    index index.html;

    location / {

        try_files $uri $uri/ /index.html;

    }

    location / {    # Gzip compression

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {

        expires 1y;        try_files $uri $uri/ /index.html;    gzip on;

        add_header Cache-Control "public, immutable";

    }    }    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

}

}

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
